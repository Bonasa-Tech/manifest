diff --git a/benchmarking/tests/utils/runner.rs b/benchmarking/tests/utils/runner.rs
index d7d913a..a9ea6c9 100644
--- a/benchmarking/tests/utils/runner.rs
+++ b/benchmarking/tests/utils/runner.rs
@@ -21,8 +21,9 @@ use wrapper::{
 };
 
 use super::{
-    match_order_in_wrapper_orders_tree, match_order_in_wrapper_orders_tree_for_cancel_up_to,
-    obj_get_u64, parse_cancel_order_order_data, parse_cancel_up_to_order_order_data,
+    cancel_all_wrapper_orders, match_order_in_wrapper_orders_tree,
+    match_order_in_wrapper_orders_tree_for_cancel_up_to, obj_get_u64,
+    parse_cancel_order_order_data, parse_cancel_up_to_order_order_data,
     parse_ioc_decoded_data, parse_limit_decoded_data, parse_post_only_decoded_data,
     parse_transaction_object, value_get_obj, value_get_str, value_get_u64, Obj, ParsedTransaction,
     PhoenixIOCOrderPacket, PhoenixLimitOrderPacket, PhoenixPostOnlyOrderPacket, Side, TestFixture,
@@ -61,7 +62,6 @@ pub async fn run_transactions(transactions_filepath: String, test_fixture: TestF
         let mut direct_place_orders: Vec<PlaceOrderParams> = Vec::new();
         let mut wrapper_place_orders: Vec<WrapperPlaceOrderParams> = Vec::new();
         let mut wrapper_cancel_orders: Vec<WrapperCancelOrderParams> = Vec::new();
-        let mut cancel_all: bool = false;
 
         let wrapper_state: &Pubkey = test_fixture.trader_to_wrapper.get(&payer).unwrap();
 
@@ -155,10 +155,24 @@ pub async fn run_transactions(transactions_filepath: String, test_fixture: TestF
                     .await;
                 }
                 "CancelAllOrdersWithFreeFunds" => {
-                    cancel_all = true;
+                    wrapper_cancel_orders.extend(
+                        cancel_all_wrapper_orders(
+                            &mut program_test_context,
+                            wrapper_state,
+                            market_key,
+                        )
+                        .await,
+                    );
                 }
                 "CancelAllOrders" => {
-                    cancel_all = true;
+                    wrapper_cancel_orders.extend(
+                        cancel_all_wrapper_orders(
+                            &mut program_test_context,
+                            wrapper_state,
+                            market_key,
+                        )
+                        .await,
+                    );
                 }
                 "CancelMultipleOrdersById" => {
                     let decoded_data_object: &Obj = value_get_obj(&instruction, "decodedData");
@@ -180,15 +194,14 @@ pub async fn run_transactions(transactions_filepath: String, test_fixture: TestF
             }
         }
 
-        if !wrapper_cancel_orders.is_empty() || !wrapper_place_orders.is_empty() || cancel_all {
+        if !wrapper_cancel_orders.is_empty() || !wrapper_place_orders.is_empty() {
             let num_orders: usize = wrapper_cancel_orders.len() + wrapper_place_orders.len();
             println!(
-                "Sending batch update for {} {} {} {} {}",
+                "Sending batch update for {} {} {} {}",
                 original_pubkey,
                 signature,
                 wrapper_cancel_orders.len(),
-                wrapper_place_orders.len(),
-                cancel_all
+                wrapper_place_orders.len()
             );
             // TODO: Lookup trader index hint
             let batch_update_ix: Instruction = batch_update_instruction(
@@ -196,7 +209,7 @@ pub async fn run_transactions(transactions_filepath: String, test_fixture: TestF
                 &payer,
                 wrapper_state,
                 wrapper_cancel_orders,
-                cancel_all,
+                false,
                 wrapper_place_orders,
             );
             let budget_ix: Instruction =
diff --git a/benchmarking/tests/utils/utils.rs b/benchmarking/tests/utils/utils.rs
index 26e960f..6c2859b 100644
--- a/benchmarking/tests/utils/utils.rs
+++ b/benchmarking/tests/utils/utils.rs
@@ -50,6 +50,38 @@ fn collect_open_orders(
     }
 }
 
+/// Turn a Phoenix cancel-all into explicit cancels for the orders represented
+/// in this benchmark's wrapper account. The fixture places its initial and
+/// replayed resting orders through the wrapper; its direct swaps are IOC.
+pub async fn cancel_all_wrapper_orders(
+    context: &mut ProgramTestContext,
+    wrapper: &Pubkey,
+    market: &Pubkey,
+) -> Vec<WrapperCancelOrderParams> {
+    let wrapper_account: Account = context
+        .banks_client
+        .get_account(*wrapper)
+        .await
+        .expect("Fetch wrapper")
+        .expect("Wrapper is not none");
+    let (fixed_data, wrapper_dynamic_data) =
+        wrapper_account.data.split_at(size_of::<ManifestWrapperStateFixed>());
+    let wrapper_fixed: &ManifestWrapperStateFixed = get_helper(fixed_data, 0);
+    let market_infos_tree: MarketInfosTreeReadOnly = unsafe {
+        MarketInfosTreeReadOnly::new(
+            wrapper_dynamic_data,
+            wrapper_fixed.market_infos_root_index,
+            NIL,
+        )
+    };
+    let market_info_index: DataIndex = market_infos_tree
+        .lookup_index(&MarketInfo::new_empty(market.to_bytes().into(), NIL));
+    collect_open_orders(wrapper_dynamic_data, market_info_index)
+        .into_iter()
+        .map(|order| WrapperCancelOrderParams::new(order.get_client_order_id()))
+        .collect()
+}
+
 pub fn get_market_info_index_for_market(wrapper_data: &mut [u8], market_key: &Pubkey) -> DataIndex {
     let (fixed_data, wrapper_dynamic_data) =
         wrapper_data.split_at_mut(size_of::<ManifestWrapperStateFixed>());
